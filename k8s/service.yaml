apiVersion: v1
kind: Service
metadata:
  name: cache-simulator-service
spec:
  selector:
    app: cache-simulator
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: NodePort